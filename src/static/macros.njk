---
permalink: false
---

{% macro renderIcons(svgs) %}
    {% for svg in svgs %}
        {{ renderIcon(svg) }}
    {% endfor %}
{% endmacro %}

{% macro renderIcon(svg) %}
    <img src="/assets/icons/tech/{{ svg }}.svg" class="icon" alt="{{ svg }}" title="{{ svg }}">
{% endmacro %}
