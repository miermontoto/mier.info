<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% if desc %}
        <meta name="description" content="{{ desc }}">
    {% else %}
        <meta name="description" content="personal portfolio website where i write about myself and what i've done in the past.">
    {% endif %}
    {% if keywords %}
        <meta name="keywords" content="{{ keywords }}">
    {% else %}
        <meta name="keywords" content="mier, Mier, Juan Mier, Juan Francisco Mier Montoto, Mier Montoto, miermontoto">
    {% endif %}
    <meta name="author" content="Juan Mier">
    <link rel="stylesheet" href="/static/css/global.css">
    <script src="/static/js/global.js" defer></script>
    <link rel="icon" href="/assets/favicon.svg" type="image/x-icon">
    <title>{{ title }}</title>
    {% if js %}
        <script src="{{ js }}" defer></script>
    {% endif %}
    {% if css %}
        <link rel="stylesheet" href="{{ css }}">
    {% endif %}
</head>
<body>
    {{ content | safe }}
</body>
</html>
