<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="generator" content="Eleventy">
    <meta name="description" content="{{ desc or "personal portfolio website that collects all my projects and professional experiences." }}">
    {% if keywords %}
        <meta name="keywords" content="{{ keywords }}">
    {% else %}
        {% keywords %}
    {% endif %}
    <meta name="author" content="Juan Mier">
    <link rel="stylesheet" href="/static/css/global.css">
    <script src="/static/js/global.js" defer></script>
    <link rel="icon" href="/assets/favicon.svg" type="image/x-icon">
    <title>{{ title or "mier" }}</title>
    {% if js %}
        <script src="{{ js }}" defer></script>
    {% endif %}
    {% if css %}
        <link rel="stylesheet" href="{{ css }}">
    {% endif %}
</head>
<body>
    <div id="browser-info">
        <span id="general-info"></span>
        <span id="window-info"></span>
        <span id="time-info"></span>
        <span id="click-info"></span>
    </div>

    {{ content | safe }}

    <a id="version-tag" href="https://github.com/miermontoto/miermontoto" target="_blank">{% version %}</span>
</body>
</html>
